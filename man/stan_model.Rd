% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_model.R
\name{stan_model}
\alias{stan_model}
\title{Create a Stan model object}
\usage{
stan_model(
  stan_file = NULL,
  code = NULL,
  compile = TRUE,
  model_name = NULL,
  include_paths = NULL,
  user_header = NULL,
  cpp_options = list(),
  stanc_options = list(),
  force_recompile = getOption("stanr_force_recompile", FALSE),
  precompiled_headers = TRUE,
  quiet = TRUE,
  external_cpp = NULL,
  use_opencl = FALSE,
  compile_standalone = FALSE,
  backend = "compiled"
)
}
\arguments{
\item{stan_file}{(string) The path to a \code{.stan} file containing a Stan
program. If \code{stan_file} is not specified then \code{code} must be specified.}

\item{code}{(string) A Stan program as a single string. If \code{code} is not
specified then \code{stan_file} must be specified.}

\item{compile}{(logical) Should the model be compiled? The default is
\code{TRUE}. If \code{FALSE} compilation can be done later via the
\code{\link[=model-method-compile]{$compile()}} method.}

\item{model_name}{(string) The name to use for the model. If \code{NULL} (the
default), the model name is derived from the Stan file name (if provided)
or set to \code{"model"}.}

\item{include_paths}{(character vector) Paths to directories where Stan
should look for files specified in \verb{#include} directives.}

\item{user_header}{(string) A path to a single C++ header file to prepend to
the generated model code. A legacy alias for \code{external_cpp}; supplied
headers are appended to \code{external_cpp} and treated identically.}

\item{cpp_options}{(list) C++ compilation options, merged into the
Makevars flags used to compile the model. Each element is either:
\itemize{
\item A named element, e.g. \code{list(CXX = "g++")}. This \emph{overrides} any value
computed internally for that name (e.g. \code{CXXFLAGS}), replacing it
rather than adding to it.
\item An unnamed string of the form \code{"<NAME> = <value>"}, e.g.
\code{"CXX = g++"}. Equivalent to the named form above -- also overrides.
\item An unnamed string of the form \code{"<NAME> += <value>"}, e.g.
\code{"CXXFLAGS += -Wno-psabi"}. \emph{Appends} \verb{<value>} to any existing value
for \verb{<NAME>} instead of replacing it (space-separated), matching how
\verb{+=} behaves in a Makevars file.
}

Entries are applied in list order, so the same name may appear more
than once, e.g. \code{list(CXXFLAGS = "-O3", "CXXFLAGS += -Wall")} first
overrides \code{CXXFLAGS}, then appends to it.}

\item{stanc_options}{(list) Stan-to-C++ transpiler options. Not yet supported.}

\item{force_recompile}{(logical) Should the model be recompiled even if it
has not been modified? The default is \code{FALSE}, but can be set via the
\code{stanr_force_recompile} option.}

\item{precompiled_headers}{(logical) Should precompiled headers be used to
speed up compilation? The default is \code{TRUE}. Automatically disabled when
\code{cpp_options} overrides compiler flags (e.g. \code{CXXFLAGS}), since the
precompiled header is not built with those flags.}

\item{quiet}{(logical) Should verbose output from compilation be suppressed?
The default is \code{TRUE}.}

\item{external_cpp}{(character vector) Paths to C++ files to prepend to the
generated model code. Useful for defining custom functions.}

\item{use_opencl}{(logical) Should the model be compiled with OpenCL
support? The default is \code{FALSE}. When \code{TRUE}, \code{stanc} generates
OpenCL-accelerated code for the functions that support it (most notably
\code{bernoulli_logit_glm} and other GLM likelihoods), and the compiled model
can run its computation on an OpenCL device. Which platform/device is
used at runtime is controlled by the \code{opencl_ids} argument of the fit
methods (e.g. \code{\link[=model-method-sample]{$sample()}}), not by this argument;
\code{opencl_ids = NULL} (the default there) means \code{select_opencl_device()}
is simply never called, so the platform/device baked in at compile time
(0/0) is used. The default OpenCL link flags are \code{"-framework OpenCL"}
on macOS and \code{"-lOpenCL"} elsewhere.}

\item{compile_standalone}{(logical) Should the Stan program's \code{functions}
block be exposed as part of this compilation? The default is \code{FALSE}.
When \code{TRUE}, \verb{$functions} is already populated when \code{stan_model()}
returns -- equivalent to \code{FALSE} here plus calling
\code{\link[=model-method-expose-stan-functions]{$expose_stan_functions()}}
immediately after, but without a second compile. See
\code{\link[=model-method-expose-stan-functions]{$expose_stan_functions()}} for
what gets exposed and how.}

\item{backend}{(string) Either \code{"compiled"} (the default), which compiles
the model to a native shared library, or \code{"stanli"}, which interprets
the model instead of compiling it. The \code{"stanli"} backend supports
constrained-scale initialization, but not \code{use_opencl}, \code{external_cpp},
or non-empty \code{cpp_options}. Its exposed-function backend supports
deterministic integer/real scalar and container functions, but not RNG,
\verb{_lp}, void, complex, or tuple functions.}
}
\value{
A \code{\link{StanModel}} object.

The compiled model is cached persistently on disk as the compiled shared
library itself, next to \code{stan_file} (named after it, e.g. \code{mymodel.stan}
gets a sibling \code{.mymodel.so}/\code{.mymodel.dll}), or under \code{\link[=tempdir]{tempdir()}} when
the model was created from a \code{code} string, or when \code{stan_file}'s
directory isn't writable. The library embeds a hash of the generated
C++, so it's reused across R sessions as long as the Stan program,
\code{include_paths}, \code{external_cpp}, and installed stanr/Stan versions are
unchanged, and is silently recompiled and overwritten otherwise --
there is no separate cache-clearing step. On a cache hit, the
Stan-to-C++ transpiler is skipped entirely, so any transpiler warnings
(e.g. from pedantic mode or deprecated syntax) are only surfaced the
first time a given model is compiled, not on subsequent cache hits.
}
\description{
Create a new \code{\link{StanModel}} object from a Stan program file or
from Stan code as a string. The \code{\link{StanModel}} object stores the Stan
program source and compiled model, and provides methods for fitting the
model using Stan's inference algorithms.

See the \code{compile} argument for control over whether and how compilation
happens.
}
\examples{
\dontrun{
# Create a StanModel from Stan code
mod <- stan_model(
  code = "
    parameters {
      real theta;
    }
    model {
      theta ~ normal(0, 1);
    }
  "
)
mod$model_name()
mod$variables()

# Run MCMC sampling
fit <- mod$sample(data = list(), chains = 2)
fit$summary()
}

}
\seealso{
\code{\link{StanModel}}, \code{\link[=model-method-compile]{$compile()}},
\code{\link[=model-method-sample]{$sample()}}
}
